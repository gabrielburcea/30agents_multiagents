[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "30agents_multiagents"
version = "0.1.0"
description = "Building 30 agents"
dependencies = [
    # Add any external pio packages

]
[tool.setuptools.packages.find]
where = ['.']
